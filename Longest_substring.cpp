#include <bits/stdc++.h>
using namespace std;
void LPS(string str, string reverse, int i, int j)
{
}
int main()
{
    string str = "forgeeksskeegfor";
    string rStr;
    reverse(str.begin(), str.end());
    LPS(str, rStr, 0, 0);
}